/* app/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-background text-white;
  }
}

@layer components {
  .btn-primary {
    @apply bg-accent text-white px-6 py-3 rounded-lg font-semibold hover:bg-green-600 transition-colors duration-200;
  }
  
  .btn-secondary {
    @apply bg-primary text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-900 transition-colors duration-200;
  }
  
  .card {
    @apply bg-dark border border-gray-800 rounded-xl p-6 shadow-lg;
  }
}